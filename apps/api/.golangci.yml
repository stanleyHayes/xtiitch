version: "2"

run:
  tests: true

linters:
  default: none
  enable:
    - bodyclose
    - errcheck
    - funlen
    - gocognit
    - gocyclo
    - gosec
    - govet
    - ineffassign
    - lll
    - misspell
    - nakedret
    - revive
    - rowserrcheck
    - sqlclosecheck
    - staticcheck
    - unconvert
    - unparam
    - unused

  settings:
    funlen:
      lines: 80
      statements: 50
    gocognit:
      min-complexity: 15
    gocyclo:
      min-complexity: 15
    lll:
      line-length: 170
    revive:
      rules:
        - name: early-return
        - name: unused-parameter

  exclusions:
    generated: lax
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling
    rules:
      # Don't gate size/complexity linters on test files: table-driven tests are
      # legitimately long/branchy. Applies to all *_test.go (unit + integration).
      - linters:
          - funlen
          - gocognit
          - gocyclo
        path: _test\.go
      # Interface fakes and table helpers intentionally carry parameters that a
      # particular test does not inspect.
      - linters:
          - revive
          - unparam
        path: _test\.go
    paths:
      - third_party$
      - builtin$
      - examples$

issues:
  max-issues-per-linter: 0
  max-same-issues: 0

formatters:
  enable:
    - gofmt
    - goimports
  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$
